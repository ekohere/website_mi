@extends('admin.index')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">comment {{ $comment->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('admin/comment/' . $comment->id . '/edit') }}" class="btn btn-primary btn-xs" title="Edit comment"><span class="glyphicon glyphicon-pencil" aria-hidden="true"/></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/comment', $comment->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<span class="glyphicon glyphicon-trash" aria-hidden="true"/>', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete comment',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $comment->id }}</td>
                                    </tr>
                                    <tr><th> Content </th><td> {{ $comment->content }} </td></tr><tr><th> Like </th><td> {{ $comment->like }} </td></tr><tr><th> Name Guest </th><td> {{ $comment->name_guest }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection